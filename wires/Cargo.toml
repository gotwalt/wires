[package]
name = "wires"
version.workspace = true
edition.workspace = true

[[bin]]
name = "wires"
path = "main.rs"

[dependencies]
library = { workspace = true }
clap = { workspace = true }
iroh = { workspace = true }
tokio = { workspace = true }
anyhow = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }

# Lockfile pins for iroh's `ed25519-dalek 3.0.0-pre.1` crypto cluster (not used
# directly; see the root Cargo.toml note). Intentionally absent from BUILD deps.
ed25519 = { workspace = true }
signature = { workspace = true }
pkcs8 = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
